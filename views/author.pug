extends layout 
block content 
    h1= title 

    if list_author.length 
        ul 
        each author in list_author
            li 
            a(href=author.url) #{author.name}
            | (#{author.date_of_birth} - #{author.date_of_death})
    else 
        p There is not author